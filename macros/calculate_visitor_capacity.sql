{% macro calculate_visitor_capacity(visitor_count_column, max_capacity) %}
    -- TODO: Calculate (visitor_count / max_capacity) * 100
    -- Handle division by zero, round to 2 decimals
    0.0
{% endmacro %}

