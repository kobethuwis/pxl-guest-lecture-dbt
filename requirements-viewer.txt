# Optional local data viewer (Streamlit). Install on top of requirements.txt.
# Python 3.8+ (same as the main project README). Use a recent pip so wheels resolve
# (pandas 2.1+ needs Python 3.9+; on 3.8, pip will install pandas 2.0.x).
streamlit>=1.28.0,<2.0.0
pandas>=2.0.0
duckdb>=0.10.0
